<h1>Admin::Users#index</h1>
<p>Find me in app/views/admin/users/index.html.erb</p>

<ul>
  <% @users.each do |user| %>
  <li>
    <%= user.name %>
    <%= link_to "削除", user, method: :delete, data: {confirm: "ユーザを削除します"} %>
  </li>
  <% end %>
</ul>